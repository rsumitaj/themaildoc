# Move to .github/workflows/deploy-content.yml to auto-deploy the site when the
# content engine (or you) commits a post. Set repo secrets CLOUDFLARE_API_TOKEN
# and CLOUDFLARE_ACCOUNT_ID first.
name: Deploy site on content change
on:
  push:
    branches: [main]
    paths: ['apps/web/src/content/**']
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @maildoc/web build
      - name: Deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: pnpm --filter @maildoc/web exec wrangler deploy
      - name: IndexNow
        run: pnpm indexnow
